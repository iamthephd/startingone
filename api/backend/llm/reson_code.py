def get_reason_code(df, file_name):
    """
    Get reason codes for the data
    """
    # Example implementation - replace with actual logic
    try:
        # Mock implementation
        return [(0, "Y/Y %", 10.5), (1, "Q/Q %", 5.2)]
    except Exception as e:
        print(f"Error getting reason code: {str(e)}")
        return []
